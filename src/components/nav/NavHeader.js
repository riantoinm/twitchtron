"use strict";
import React from "react";

/** Defines the header of the navigation panel */
class NavHeader extends React.Component {
    render() {
        return(
          <header>
              <img id="nav-twitch-logo"/>
          </header>
        );
    }
}

export default NavHeader;